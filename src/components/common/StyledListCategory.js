import styled from "styled-components";

const StyledListCategory = styled.p`
  font-size: 0.8em;
  font-family: ${({ theme }) => theme.fontSans};
  font-weight: 700;
  text-transform: uppercase;
  margin-top: 2rem;
  margin-bottom: 1rem;
`;

export default StyledListCategory;